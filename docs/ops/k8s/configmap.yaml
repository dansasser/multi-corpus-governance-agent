apiVersion: v1
kind: ConfigMap
metadata:
  name: mcg-config
  namespace: mcg-agent
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  LOG_FORMAT: "json"
  GEN_PROVIDER: "punctuation_only"
  METRICS_ENABLED: "true"
  PORT: "8000"

