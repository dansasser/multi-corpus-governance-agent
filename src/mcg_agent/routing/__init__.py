"""Routing pipeline that follows the documented Routing Matrix.

This module exposes a `GovernedAgentPipeline` skeleton that sequences stages
exactly as defined in `routing-matrix.md` via `protocols.routing_protocol`.
"""

